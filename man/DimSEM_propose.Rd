% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DimSEM_propose.R
\name{DimSEM_propose}
\alias{DimSEM_propose}
\title{Propose a dimensionality-sensitive SEM schematic}
\usage{
DimSEM_propose(
  data,
  items = NULL,
  methods = "EGA",
  item_names = NULL,
  ega_args = list(),
  pa_args = list(),
  ekc_args = list(),
  partition_source = c("auto", "EGA", "PA", "EKC"),
  assignment_args = list(),
  fit_cfa = TRUE,
  cfa_source = c("selected", "EGA", "PA", "EKC"),
  cfa_args = list(),
  propose_hyper = TRUE,
  hyper_args = list(),
  audit_orphans = TRUE,
  orphan_args = list(),
  parallel_hyper = TRUE,
  parallel_hyper_ncores = NULL,
  make_plots = TRUE,
  plot_args = list(),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or matrix with rows as cases/respondents. If
\code{items} is left \code{NULL}, every column is treated as a manifest item and
must be numeric. If \code{items} is supplied, \code{data} may be a raw data set
containing additional (even non-numeric) columns; only the selected item
columns are checked and analyzed.}

\item{items}{Optional character vector of column names (or integer vector
of column positions) selecting the subset of columns in \code{data} that
represent manifest items. Defaults to \code{NULL}, in which case all columns
of \code{data} are passed downstream.}

\item{methods}{Character vector determining methods to run. Any combination of \code{"EGA"},
\code{"PA"}, \code{"EKC"}, or \code{"all"} is accepted, with \code{"EGA"} used by default.}

\item{item_names}{Optional character vector of display names for the
analyzed items. If supplied, must have length equal to the number of
analyzed items (i.e., \code{length(items)} when \code{items} is used, otherwise
\code{ncol(data)}). Defaults to the selected column names or generated names
of the form \code{item_1}, \code{item_2}, ...}

\item{ega_args}{Optional named list of arguments passed to \code{\link[EGAnet:EGA]{EGAnet::EGA()}}.
Defaults are merged with \code{list(corr = "auto", model = "glasso", algorithm = "louvain", consensus.method = "lowest_tefi", uni.method = "LE", plot.EGA = FALSE)}. By default, DimSEM additionally
enforces a minimum community size of three items via Louvain
resolution annealing: whenever the EGA solution contains a community
of fewer than \code{min_community_size} (default 3) items that is connected
to the rest of the network, the Louvain \code{resolution} parameter is
lowered from \code{resolution_start} (default 1.2) in decrements of
\code{resolution_step} (default 0.01), coarsening the partition, until all
communities reach the minimum size or \code{resolution_min} (default 0.05)
is hit. Sub-threshold communities that form their own disconnected
network component (isolated items or isolated doublets) can never be
merged by coarsening and are instead converted to unassigned orphans
(NA), as are any communities still below the threshold at the
resolution floor; orphaned items are excluded from the proposed
measurement syntax and SEM schematic. Rationale: a two-item community
cannot identify a latent dimension (the three-indicator rule; Gorsuch,
1997), consistent with the two-node-community handling in Taxonomic
Graph Analysis (Samo et al., 2025) and with the hyper-level
\code{min_block_size} rule. All four annealing controls are intercepted
convenience arguments and are not passed to \code{\link[EGAnet:EGA]{EGAnet::EGA()}}. Set
\code{min_community_size = 1} to disable the rule, or supply a fixed
\code{resolution} (or a non-Louvain \code{algorithm}, e.g. \code{"walktrap"}) to
disable the search while retaining orphan conversion.}

\item{pa_args}{Optional named list of arguments passed to
\code{\link[psych:fa.parallel]{psych::fa.parallel()}}. Set \code{n_cores} inside this list to temporarily set
\code{options(mc.cores = n)} during the PA call. Defaults are merged with
\code{list(fm = "minres", fa = "fa", n.iter = 100, quant = .95, cor = "cor", use = "pairwise", plot = FALSE)}. A second convenience argument,
\code{suppress_heywood} (default \code{TRUE}), muffles the "(ultra-)Heywood case"
warnings that \code{\link[psych:fa.parallel]{psych::fa.parallel()}} emits while deliberately
overextracting factors from observed and resampled correlation matrices;
these throwaway EFA solutions do not affect the eigenvalue-based
retention decision. All other warnings are passed through. Set
\code{suppress_heywood = FALSE} to surface them.}

\item{ekc_args}{Optional named list of arguments for the Empirical Kaiser
Criterion (Braeken & van Assen, 2017). Currently supports \code{use}, passed to
\code{\link[stats:cor]{stats::cor()}} (default \code{"pairwise.complete.obs"}). EKC compares sample
eigenvalues of the item correlation matrix against serially updated
Marchenko-Pastur reference eigenvalues (floored at the classical Kaiser
value of 1) and retains dimensions until the first eigenvalue falls below
its reference. It is deterministic, closed-form, and well suited to
Likert-type item data. A raw retention count of 0 (e.g., pure-noise data)
is reported as such in \code{results$EKC$n_dim_raw} but clamped to 1 for
partition construction.}

\item{partition_source}{Character scalar. Which method should define the
final item partition? \code{"auto"} prefers EGA, then PA, then EKC. \code{"EGA"}
uses the EGA community vector directly. \code{"PA"} or \code{"EKC"} uses the estimated
number of dimensions and assigns items by partitioning the regularized
partial-correlation network into exactly that many communities (weighted
walktrap by default; see \code{assignment_args}).}

\item{assignment_args}{Optional named list controlling how PA/EKC
dimension counts are turned into item partitions. Supports \code{algorithm},
one of \code{"walktrap"} (default; \code{\link[igraph:cluster_walktrap]{igraph::cluster_walktrap()}} with edge
weights, cut at exactly the requested number of communities via
\code{\link[igraph:communities]{igraph::cut_at()}}), \code{"fast_greedy"} (\code{\link[igraph:cluster_fast_greedy]{igraph::cluster_fast_greedy()}}
with weights, also cut via \code{cut_at()}), or \code{"fluid"}
(\code{\link[igraph:cluster_fluid_communities]{igraph::cluster_fluid_communities()}}; ignores edge weights and is
retained only for backward compatibility); \code{glasso_args} for arguments passed to
\code{\link[qgraph:EBICglasso]{qgraph::EBICglasso()}} when an EGA network is unavailable; \code{cor = "auto"}
or \code{"pearson"} for the correlation matrix used by the glasso fallback;
\code{use} for \code{stats::cor()} when \code{cor = "pearson"}; \code{weight_transform}
(\code{"absolute"} or \code{"positive"}) for turning the regularized signed matrix
into graph topology; and \code{connect_disconnected}, a logical that allows tiny
bridge edges to be added when the regularized network is disconnected.}

\item{fit_cfa}{Logical. If \code{TRUE} (default), fit a confirmatory factor
analysis with \code{\link[lavaan:cfa]{lavaan::cfa()}} using the proposed item partition as the
measurement model and store it in the \code{cfa} element of the returned
object, together with the estimated inter-factor correlation matrix and
standard fit measures. Requires the \code{lavaan} package.}

\item{cfa_source}{Character scalar. Which item partition should define the
CFA measurement model? \code{"selected"} (default) uses the partition already
chosen via \code{partition_source} -- under auto-selection this is the EGA
solution whenever EGA was run and produced item communities. \code{"EGA"},
\code{"PA"}, or \code{"EKC"} instead force the CFA onto that specific method's
partition, which may differ from the reported \code{selected} partition.}

\item{cfa_args}{Optional named list of arguments passed to \code{\link[lavaan:cfa]{lavaan::cfa()}}.
Defaults are merged with \code{list(std.lv = TRUE, ordered = TRUE, missing = "pairwise")}:
\code{std.lv = TRUE} fixes latent variances to 1 so that all loadings are
estimated and latent covariances are correlations; \code{ordered = TRUE}
treats all items as ordered-categorical (Likert-type or binary), making
lavaan estimate the model from polychoric/tetrachoric correlations with
a WLSMV-type estimator, and the reported fit measures are then the
robust (scaled) versions. Pass \code{list(ordered = FALSE)} to treat items as
continuous instead. Items left unassigned by the partition (NA
dimension) are excluded from the measurement model.}

\item{propose_hyper}{Logical. If \code{TRUE} (default), evaluate whether the
proposed level-1 dimensions warrant one or more second-order
("hyper") factors, using network-psychometric criteria only. Results
are stored in the \code{hyper} element of the returned object.}

\item{hyper_args}{Optional named list controlling the hyper-structure
evaluation, which uses a permutation-validated higher-order Louvain:
the null hypothesis that level-1 communities are mutually independent
conditional on their internal structure is simulated exactly by
permuting data rows independently within each community's column
block; the between-community edge mass of the re-estimated
regularized network (the between-community component of the
weighted-modularity numerator) is the test statistic; BH-FDR-validated
community pairs form a meta-graph on which Louvain consensus
clustering identifies hyper-blocks, with unconnected dimensions
remaining stand-alone. Options: \code{n_perm} permutations (default:
adaptive, \code{max(500, ceiling(2 * n_pairs / alpha))}, so that a single
true between-community connection can survive FDR correction);
\code{alpha} (default .05) FDR level and omnibus gate; \code{min_block_size}
(default 3) the minimum number of level-1 dimensions required to
support a hyper-factor -- a hyper-factor over two dimensions is an
exact, zero-df reparameterization of their correlation and hence
unfalsifiable, so validated groupings below this size are reported as
correlated level-1 dimensions instead; \code{member_rule} (\code{"majority"},
the default, or \code{"all"}) the family-resemblance membership criterion:
a dimension is retained in a candidate hyper-community only if its
MARGINAL between-dimension dependence with the required share of
co-members is individually permutation-validated, with weakly
attached "stray" dimensions removed by backward elimination and
reported in \verb{$hyper$permutation$evicted}; \code{consensus_reps} (default
500) Louvain consensus repetitions; \code{resolution} (default 1) Louvain
resolution; \code{glasso_args} passed to \code{\link[qgraph:EBICglasso]{qgraph::EBICglasso()}}.}

\item{audit_orphans}{Logical; if \code{TRUE} (default), validate every item's
membership in its assigned community AFTER partition selection, and
convert failures to unassigned orphans (\code{NA}). Community-detection
algorithms return complete partitions of connected nodes: EGA assigns
\code{NA} only to fully isolated items, so a weakly connected item -- even
a single hairline regularized edge -- is always absorbed into some
community regardless of seed. The audit is therefore applied to the
selected partition irrespective of its source (EGA, PA, or EKC).
Orphaned items are excluded from the proposed measurement syntax, SEM
schematic, and hyper-structure evaluation; details are stored in the
\code{orphan_audit} element.}

\item{orphan_args}{Optional named list controlling the audit. \code{method}:
\code{"permutation"} (default) retains an item only if its marginal
item-to-community correlation mass beats a column-permutation null at
\code{alpha} (default .05; \code{n_perm}, default 200, permutations; exact and
assumption-free, consistent with the hyper-level machinery);
\code{"loading"} retains an item only if its standardized network loading
on its own community reaches \code{min_loading} (default 0.15, the
small-effect benchmark of Christensen & Golino, 2021); \code{"none"}
disables the audit. After eviction, communities left with fewer than
\code{min_community_size} (default 3) items are dissolved to orphans as
well, consistent with the three-indicator identification rule used
throughout the package.}

\item{parallel_hyper}{Logical. If \code{TRUE} (default), evaluate the
permutation replicates of the hyper-structure test in parallel on a
\code{doSNOW} cluster. The permutation loop is the dominant cost of
\code{DimSEM_propose()} (each replicate re-estimates an \code{\link[qgraph:EBICglasso]{qgraph::EBICglasso()}}
network, and the default \code{n_perm} grows with the number of level-1
dimension pairs), and replicates are mutually independent, so the
speed-up is close to linear in the number of cores. Requires the
\code{foreach}, \code{doSNOW}, and \code{parallel} packages; if any is unavailable, or
if only one core is usable, the test falls back to serial evaluation
with a warning. RESULTS ARE UNAFFECTED: all permutation indices are
drawn serially from the master RNG stream before any work is
dispatched, and workers perform no random number generation, so the
output for a given \code{seed} is bit-identical to serial evaluation
regardless of \code{parallel_hyper} or the number of cores.}

\item{parallel_hyper_ncores}{Integer or \code{NULL}. Number of worker
processes to use when \code{parallel_hyper = TRUE}. Defaults to \code{NULL},
which resolves to \code{parallel::detectCores() - 1}, leaving one core for
background tasks. The value is clamped to the number of available
cores and never exceeds the number of permutation replicates.}

\item{make_plots}{Logical. If \code{TRUE}, create a community/network plot and a
simple directed SEM schematic.}

\item{plot_args}{Optional named list controlling plotting. Currently supports
\code{network_layout}, \code{sem_layout}, \code{node_size}, and \code{label_size}.}

\item{seed}{Optional integer seed applied before stochastic procedures.}

\item{verbose}{Logical. If \code{TRUE}, print method-progress messages.}
}
\value{
An object of class \code{"DimSEM_proposal"}, a list with the following
elements:
\describe{
\item{results}{Raw and summarized method-specific results for EGA, PA, and/or EKC. PA/EKC results include their graph-partition item allocations.}
\item{selected}{The selected partition source, dimension count, item partition table, and lavaan-style measurement syntax.}
\item{cfa}{When \code{fit_cfa = TRUE}: the partition source used, the model syntax, the fitted \code{\link[lavaan:cfa]{lavaan::cfa()}} object (\code{fit}), a convergence flag, the inter-factor correlation matrix (\code{factor_cor}), and selected fit measures. \code{NULL} when \code{fit_cfa = FALSE}.}
\item{hyper}{When \code{propose_hyper = TRUE}: the hyper-structure result (verdict, hyper-blocks, stand-alone dimensions, evicted strays, permutation pair table and global p-value) and a plain-language recommendation. \code{NULL} when \code{propose_hyper = FALSE}.}
\item{plots}{A list containing \code{network} and \code{sem} ggplot/ggraph objects when \code{make_plots = TRUE}.}
\item{graphs}{Underlying igraph objects for the item network and SEM schematic.}
\item{data_info}{Basic information about the analyzed item matrix.}
}
}
\description{
\code{DimSEM_propose()} runs one or more dimensionality-detection procedures and
returns a proposed item-to-dimension partition that can later be converted
into a hierarchical SEM model. By default, the proposal is based on
Exploratory Graph Analysis (EGA), which directly returns item communities. If
Parallel Analysis (PA) or the Empirical Kaiser Criterion (EKC) is
requested, the function estimates the number of dimensions and then partitions the regularized item
partial-correlation network into exactly that many communities using
weighted walktrap community detection (\code{\link[igraph:cluster_walktrap]{igraph::cluster_walktrap()}} +
\code{\link[igraph:communities]{igraph::cut_at()}}) by default; see \code{assignment_args} for alternatives.
}
\examples{
\dontrun{
proposal <- DimSEM_propose(
  data = my_items,
  items = c("q1", "q2", "q3", "q4", "q5", "q6"),
  methods = c("EGA", "PA", "EKC"),
  pa_args = list(n.iter = 500, n_cores = 8, fa = "fa", plot = FALSE),
  ega_args = list(corr = "auto", model = "glasso", algorithm = "walktrap")
)

proposal
proposal$selected$model_syntax
summary(proposal$cfa$fit, fit.measures = TRUE)
proposal$cfa$factor_cor
proposal$results$PA$partition_table
print(proposal$plots$network)
print(proposal$plots$sem)
}

}
